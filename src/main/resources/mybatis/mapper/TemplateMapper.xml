<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qwli7.blog.mapper.TemplateMapper">


    <select id="findByName" resultType="Template">
        select id, name, content,  create_at, modify_at from blog_template where name = #{name,jdbcType=VARCHAR}
    </select>

    <select id="findAll" resultType="Template">
        select id, name, content ,create_at, modify_at from blog_template
    </select>

    <delete id="deleteById" parameterType="int">
        delete from blog_template where id = #{id}
    </delete>

    <update id="update" parameterType="Template">
        update blog_template
            <set>
                <if test="name != null and name != ''">
                    name = #{name,jdbcType=VARCHAR},
                </if>
                <if test="content != null and content != ''">
                    content = #{content,jdbcType=VARCHAR},
                </if>
                <if test="modifyAt != null">
                    modify_at = {modifyAt},
                </if>
            </set>
        where id = #{id}
    </update>

    <insert id="insert" parameterType="Template" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        <selectKey keyColumn="id" keyProperty="id" order="AFTER">
            select last_insert_id()
        </selectKey>
        insert into blog_template(name, content, create_at, modify_at) values (#{name,jdbcType=VARCHAR},
        #{content,jdbcType=VARCHAR},
        now(), now())
    </insert>
</mapper>